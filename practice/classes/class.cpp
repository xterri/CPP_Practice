#include "cat.hpp"

int     main(void) {
    // declare the 'type'/class here and give it a name 
        // declare like a variable
    cat catFunctions;

    // access the 'object' functions with '.'
    catFunctions.speak();
    catFunctions.jump();

    return 0;
}
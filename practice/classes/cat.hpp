// Headerfile
#ifndef CAT_H
# define CAT_H

// classes = kind of like a blueprint
// actual things create from the cat:: functions are called 'object'

// class = like declaring a new type (like structs)
class cat {
    public: // must declare so functions can be used outside the files
        void speak();
        void jump();
};

#endif